
<div class="show">
  <h1>Event information</h1>

  <div class="event-information">
    
    <div class="event-header">
      <h2 class="event-name">
        <%= @event.name %>
      </h2>
      <p class="event-description">
        <%= @event.description %>
      </p>
    </div>

    <div class="event-info">
      <p>
        <%= image_tag 'address.png' %>
        <%= @event.address %>
      </p>
      
      <p>
        <%= image_tag 'person.png' %>
        <%= link_to @event.owner.name, @event.owner %>
      </p>      
    </div>


    <p class="event-date">
      <%= validate_event_date %>
    </p>

  </div>

  <h2>List of Attendees</h2>

  <% unless @attendees.size.zero? %>
    <ol class="list">
      <% @attendees.each do |attendee| %>
        <li><%= attendee.name %></li>
      <% end %>
    </ol>
  <% else %>
    <p class="no-attendee">There are no attendees for <span><%= @event.name %></span> at this time.</p>
  <% end %>


  <div class="back-btn">
    <%= button_to 'Back to all events', events_path, method: :get %>
  </div>
</div>